#pragma once

#include "CProgramContext.h"
/*#include "Vector.h"
#include <cstdio>
#include <cstdlib>
#include <vector>
#include <fstream>
#include <float.h>*/

/*reads excel files with points in them */
class EXEreader
{
   /* public:
        EXEreader();
		~EXEreader(){}
		void parseFile(const char* argv);

		glm::vec3 gCoordinate(int index){return latLong[index];}
		size_t size(){return latLong.size();}
		f64 gDTS(int index){return dts[index];}
		f64 gMinLatitude(){return minlat;}
		f64 gMaxLatitude(){return maxlat;}
		f64 gMinLongitude(){return minlong;}
		f64 gMaxLongitude(){return maxlong;}

	private:
		FILE* readFile;
		vector<glm::vec3> latLong;
		vector<f64> dts;
		f64 prevT'
            ime; //to calculate dt with
		f64 minlat;
		f64 minlong;
		f64 maxlat;
		f64 maxlong;*/
};
